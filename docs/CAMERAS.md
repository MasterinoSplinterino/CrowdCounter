# Настройка IP-камер

## Поддерживаемые камеры

Система работает с любыми камерами, поддерживающими RTSP:
- HiWatch / Hikvision
- Dahua
- Uniview
- Axis
- и другие ONVIF-совместимые

## HiWatch DS-I853M (рекомендуемая)

### Характеристики

| Параметр | Значение |
|----------|----------|
| Разрешение | 8MP (3840×2160) |
| Угол обзора | 113° |
| ИК-подсветка | 30м |
| Питание | PoE (802.3af) |
| Протоколы | RTSP, ONVIF, HTTP |

### RTSP URL

```
# Основной поток (4K) - не рекомендуется для детекции
rtsp://admin:PASSWORD@10.0.0.101:554/Streaming/Channels/101

# Субпоток (1080p) - рекомендуется
rtsp://admin:PASSWORD@10.0.0.101:554/Streaming/Channels/102

# Третий поток (720p)
rtsp://admin:PASSWORD@10.0.0.101:554/Streaming/Channels/103
```

### Настройка через веб-интерфейс

1. Откройте `http://10.0.0.101` в браузере
2. Войдите (admin / ваш пароль)
3. **Configuration → Video/Audio → Video**
4. Для Sub Stream:
   - Resolution: 1920×1080
   - Frame Rate: 15 fps
   - Bitrate Type: Variable
   - Max Bitrate: 2048 Kbps
5. **Save**

## Сетевая схема

```
Internet
    │
    ▼
┌─────────────┐
│   Router    │
│ 192.168.1.1 │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌─────────────┐
│   Server    │     │  PoE Switch │
│ 192.168.1.10│◄────│ 10.0.0.1    │
└─────────────┘     └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌─────────┐       ┌─────────┐       ┌─────────┐
    │ Cam 101 │       │ Cam 102 │       │ Cam 103 │
    │10.0.0.101│      │10.0.0.102│      │10.0.0.103│
    └─────────┘       └─────────┘       └─────────┘
```

## Рекомендации по монтажу

### Потолочное размещение (Bird's Eye View)

Оптимальный вариант для максимальной точности.

```
         Потолок
    ┌──────────────────┐
    │       📷         │  ← Камера смотрит вниз
    │   ┌──────────┐   │
    │   │ Зона     │   │
    │   │ детекции │   │
    │   │  8×8м    │   │
    │   └──────────┘   │
    └──────────────────┘
         Пол

    Высота: 3-5 метров
    Угол: 90° (строго вниз)
    Покрытие: ~64 м² (при 4м высоты)
```

### Настенное размещение

Используйте, если потолочный монтаж невозможен.

```
    Стена
    │
    │  📷 ← Камера (45-60°)
    │ ╱
    │╱
    ├──────────────────
    │   Зона детекции
    │
```

## Тестирование камеры

### Проверка RTSP потока

```bash
# С помощью ffprobe
ffprobe -rtsp_transport tcp rtsp://admin:pass@10.0.0.101:554/Streaming/Channels/102

# С помощью VLC
vlc rtsp://admin:pass@10.0.0.101:554/Streaming/Channels/102
```

### Тест детекции

```bash
cd scripts
python test_webcam.py --camera "rtsp://admin:pass@10.0.0.101:554/Streaming/Channels/102"
```

## Типичные проблемы

### Камера недоступна

1. Проверьте ping:
   ```bash
   ping 10.0.0.101
   ```
2. Проверьте порт:
   ```bash
   nc -zv 10.0.0.101 554
   ```
3. Проверьте учётные данные в веб-интерфейсе камеры

### Задержка / лаг потока

1. Используйте субпоток вместо основного
2. Уменьшите разрешение / bitrate
3. Проверьте загрузку сети

### Артефакты на изображении

1. Проверьте качество кабеля
2. Уменьшите длину кабеля (макс 100м для Cat5e)
3. Используйте PoE extender для длинных дистанций

## Интеграция с CrowdCount

После настройки камеры:

1. Откройте CrowdCount → Settings → Залы
2. Добавьте зал с URL камеры:
   ```
   rtsp://admin:PASSWORD@10.0.0.101:554/Streaming/Channels/102
   ```
3. Камера автоматически подключится и начнёт детекцию
