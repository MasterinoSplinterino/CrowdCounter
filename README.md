# CrowdCount

Система подсчёта людей в залах конференции в реальном времени с использованием компьютерного зрения и веб-дашборда.

## Проблема

Во время конференций организаторам сложно оперативно понять загруженность залов: где свободные места, какой доклад собрал больше всего людей, когда пора открывать дополнительный зал. Ручной подсчёт невозможен при 6 параллельных секциях.

## Решение

CrowdCount анализирует видеопотоки с IP-камер через нейросеть YOLO26, считает количество людей в каждом зале и отображает данные на веб-дашборде в реальном времени (задержка < 60 секунд). Все настройки — залы, камеры, параметры детекции — управляются через веб-интерфейс.

## Ключевые возможности

- Подсчёт людей в нескольких залах одновременно
- Веб-дашборд с текущим количеством, процентом заполненности и цветовой индикацией
- Live-превью с камер (кадр с bbox-рамками вокруг людей)
- Графики загруженности за день
- Настройка залов и параметров детекции через UI
- Работает на CPU (Linux-сервер)
- Поддержка IP-камер (RTSP), веб-камер, видеофайлов

---

## Архитектура

Система состоит из двух сервисов: **Python-бэкенд** (детекция + API) и **Next.js-фронтенд** (дашборд). Всё хранится в SQLite — никаких внешних зависимостей типа Redis или PostgreSQL.

```
┌──────────────────────────────────────────────────────────────────┐
│                       LINUX SERVER                               │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                 BACKEND (Python)                            │  │
│  │                                                            │  │
│  │  ┌──────────────┐    ┌──────────────┐    ┌─────────────┐  │  │
│  │  │   Detector    │    │   FastAPI     │    │   SQLite    │  │  │
│  │  │              │    │              │    │             │  │  │
│  │  │  YOLO26n     │───►│  REST API    │◄──►│  rooms      │  │  │
│  │  │  OpenCV      │    │  WebSocket   │    │  counts     │  │  │
│  │  │  EMA smooth  │    │              │    │  settings   │  │  │
│  │  └──────┬───────┘    └──────┬───────┘    └─────────────┘  │  │
│  │         │                   │                              │  │
│  │         │  кадры с bbox     │  API :8000                   │  │
│  │         └───────────────────┘                              │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                FRONTEND (Next.js)                          │  │
│  │                                                            │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │  │
│  │  │ Dashboard │ │ Settings │ │ Room     │ │ Live Preview │  │  │
│  │  │ (залы)   │ │ (залы +  │ │ Detail + │ │ (кадр с      │  │  │
│  │  │          │ │ детекция)│ │ графики  │ │  bbox)       │  │  │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │  │
│  │                                                            │  │
│  │  WebSocket ◄──── real-time updates ────► :3000             │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
         ▲
         │ Ethernet (10.0.0.x)
         │
  ┌──────▼───────┐
  │  PoE Switch  │
  └──┬──┬──┬──┬──┘
     │  │  │  │  ... ×6
  ┌──▼┐┌▼─┐┌▼─┐┌▼─┐
  │101││102││103││104│  IP-камеры (RTSP)
  └───┘└──┘└──┘└──┘
```

### Пайплайн обработки кадра

```
RTSP / Webcam / File
        │
        ▼
  Frame Grabber ──────── 1 кадр каждые N секунд (настраивается в UI)
        │
        ▼
  Preprocess ─────────── Resize до 640×640
        │
        ▼
  YOLO26n ────────────── model.predict(frame, classes=[0])
        │                 class 0 = "person" (COCO)
        ▼                 confidence > threshold (настраивается в UI)
  Count + Smooth ──────── EMA: smoothed = α × current + (1−α) × previous
        │
        ├──► SQLite ───── INSERT (room_id, count, timestamp)
        ├──► WebSocket ── push обновление на дашборд
        └──► Frame ────── кадр с bbox → base64 → live preview в UI
```

---

## Стек технологий

| Компонент | Технология | Назначение |
|-----------|-----------|------------|
| **Детекция** | [Ultralytics YOLO26](https://docs.ultralytics.com/models/yolo26/) | Подсчёт людей (NMS-free, оптимизирован для CPU) |
| **Computer Vision** | OpenCV | Захват RTSP-потоков, обработка кадров |
| **Backend** | Python 3.11+ / FastAPI | REST API + WebSocket + детекция |
| **БД** | SQLite | Конфигурация залов, история подсчётов, настройки |
| **Frontend** | Next.js 14 / React | Дашборд, настройки, live-превью |
| **UI** | Tailwind CSS + shadcn/ui | Компоненты интерфейса |
| **Графики** | Recharts | Визуализация загруженности за день |
| **Контейнеризация** | Docker + Docker Compose | Деплой на Linux-сервер |

### Почему YOLO26

- **NMS-free** — end-to-end детекция без пост-обработки, проще код, меньше дублей
- **На 43% быстрее на CPU** чем YOLO11 (~39ms vs ~56ms на кадр)
- **Улучшенная детекция мелких объектов** (STAL) — важно при виде с потолка
- **Тот же API Ultralytics** — одна строка для смены модели

### Почему SQLite (без Redis)

- Нулевая настройка — один файл `crowdcount.db`
- Достаточно для нагрузки: 6 залов × 1 запись / 15 сек = 24 записи/мин
- Встроенный WAL-режим для одновременного чтения/записи
- Легко бэкапить — просто скопировать файл
- История и аналитика через обычные SQL-запросы

---

## Структура проекта

```
crowdcount/
├── README.md
├── docker-compose.yml
├── docker-compose.dev.yml        # Разработка (веб-камера)
├── .env.example
├── .gitignore
│
├── backend/                      # Python: детекция + API
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── main.py                   # Точка входа FastAPI
│   │
│   ├── detector/                 # Сервис детекции
│   │   ├── __init__.py
│   │   ├── engine.py             # YOLO26 inference wrapper
│   │   ├── camera.py             # Захват кадров (RTSP/Webcam/File)
│   │   ├── counter.py            # Подсчёт + EMA сглаживание
│   │   └── manager.py            # Управление потоками по залам
│   │
│   ├── api/                      # REST API
│   │   ├── __init__.py
│   │   ├── rooms.py              # CRUD залов
│   │   ├── counts.py             # Текущие значения + история
│   │   ├── settings.py           # Настройки детекции
│   │   ├── preview.py            # Live-кадры с bbox
│   │   └── ws.py                 # WebSocket для дашборда
│   │
│   ├── db/                       # База данных
│   │   ├── __init__.py
│   │   ├── database.py           # SQLite подключение + WAL
│   │   ├── models.py             # Схема таблиц
│   │   └── migrations.py         # Инициализация БД
│   │
│   └── config.py                 # Стартовая конфигурация (device: cpu)
│
├── frontend/                     # Next.js дашборд
│   ├── Dockerfile
│   ├── package.json
│   ├── next.config.js
│   ├── tailwind.config.js
│   │
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx              # Dashboard — обзор всех залов
│   │   ├── rooms/
│   │   │   └── [id]/
│   │   │       └── page.tsx      # Детали зала + график + превью
│   │   └── settings/
│   │       ├── page.tsx           # Общие настройки
│   │       ├── rooms/
│   │       │   └── page.tsx       # Управление залами (CRUD)
│   │       └── detection/
│   │           └── page.tsx       # Параметры детекции
│   │
│   ├── components/
│   │   ├── dashboard/
│   │   │   ├── RoomCard.tsx       # Карточка зала (кол-во + %)
│   │   │   ├── RoomGrid.tsx       # Сетка карточек
│   │   │   └── StatusBadge.tsx    # Цветной индикатор статуса
│   │   ├── room/
│   │   │   ├── OccupancyGauge.tsx # Круговой индикатор %
│   │   │   ├── HistoryChart.tsx   # График загруженности за день
│   │   │   └── LivePreview.tsx    # Кадр с камеры + bbox
│   │   ├── settings/
│   │   │   ├── RoomForm.tsx       # Форма добавления/редакции зала
│   │   │   └── DetectionForm.tsx  # Форма настроек детекции
│   │   └── shared/
│   │       ├── LiveIndicator.tsx  # Пульсирующий "● LIVE"
│   │       └── Navbar.tsx         # Навигация
│   │
│   ├── hooks/
│   │   ├── useWebSocket.ts        # Real-time обновления
│   │   └── useRooms.ts            # Загрузка данных залов
│   │
│   └── lib/
│       └── api.ts                 # HTTP-клиент к бэкенду
│
├── scripts/                       # Утилиты для тестирования
│   ├── test_photo.py              # Тест детекции на фото
│   ├── test_video.py              # Тест детекции на видео
│   ├── test_webcam.py             # Тест с веб-камерой (GUI)
│   └── calibrate.py               # Калибровка точности
│
└── docs/
    ├── DEPLOYMENT.md              # Инструкция деплоя на Linux
    ├── CAMERAS.md                 # Настройка камер HiWatch
    └── CALIBRATION.md             # Калибровка и тюнинг
```

---

## База данных (SQLite)

### Схема

```sql
-- Залы (управляются через UI)
CREATE TABLE rooms (
    id          TEXT PRIMARY KEY,
    name        TEXT NOT NULL,
    capacity    INTEGER NOT NULL,
    camera_url  TEXT NOT NULL,
    is_active   BOOLEAN DEFAULT 1,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- История подсчётов (для графиков и аналитики)
CREATE TABLE counts (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    room_id     TEXT NOT NULL REFERENCES rooms(id),
    count       INTEGER NOT NULL,
    raw_count   INTEGER NOT NULL,
    occupancy   REAL NOT NULL,
    timestamp   DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_counts_room_time ON counts(room_id, timestamp);

-- Настройки детекции (управляются через UI)
CREATE TABLE settings (
    key         TEXT PRIMARY KEY,
    value       TEXT NOT NULL
);

-- Дефолтные настройки
INSERT INTO settings (key, value) VALUES
    ('model', 'yolo26n.pt'),
    ('confidence_threshold', '0.35'),
    ('detection_interval', '15'),
    ('smoothing_alpha', '0.3'),
    ('imgsz', '640');
```

### Объём данных

При 6 залах и интервале 15 секунд:

| Период | Записей | Размер ~БД |
|--------|---------|-----------|
| 1 час | 1 440 | ~100 KB |
| 1 день (10 часов) | 14 400 | ~1 MB |
| 1 неделя | 100 800 | ~7 MB |
| 1 месяц | 432 000 | ~30 MB |

SQLite справляется с такой нагрузкой без каких-либо проблем.

---

## UI — Страницы и функционал

### 1. Dashboard (главная)

Обзор всех залов в виде карточек.

```
┌─────────────────────────────────────────────────────────┐
│  CrowdCount          Dashboard  |  Settings     ● LIVE  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ Главный зал │  │   Зал A     │  │   Зал B     │     │
│  │             │  │             │  │             │     │
│  │   127       │  │    85       │  │   198       │     │
│  │  / 300      │  │  / 100      │  │  / 200      │     │
│  │ ████████░░  │  │ █████████░  │  │ ██████████  │     │
│  │  42%  🟢    │  │  85%  🟠   │  │  99%  🔴   │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   Зал C     │  │   Зал D     │  │   Зал E     │     │
│  │    45       │  │    30       │  │     0       │     │
│  │  / 150      │  │  / 80       │  │  / 120      │     │
│  │ ███░░░░░░░  │  │ ████░░░░░░  │  │ ░░░░░░░░░░  │     │
│  │  30%  🟢    │  │  37%  🟢   │  │   0%  ⚪   │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
│  Обновлено: 14:32:15                                    │
└─────────────────────────────────────────────────────────┘
```

Клик на карточку → страница зала.

### 2. Страница зала

Детальная информация, live-превью и график.

```
┌─────────────────────────────────────────────────────────┐
│  ← Назад          Главный зал                   ● LIVE │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌───────────────────────┐  ┌────────────────────────┐  │
│  │                       │  │  Сейчас                │  │
│  │   Live Preview        │  │                        │  │
│  │   (кадр с камеры,     │  │     127 / 300          │  │
│  │    bbox вокруг        │  │      42.3%             │  │
│  │    людей)             │  │                        │  │
│  │                       │  │  Статус: 🟢 Свободно   │  │
│  │                       │  │  Камера: 10.0.0.101    │  │
│  └───────────────────────┘  └────────────────────────┘  │
│                                                         │
│  Загруженность за сегодня                               │
│  ┌──────────────────────────────────────────────────┐   │
│  │  300 ┤                                           │   │
│  │      │          ╭──╮                             │   │
│  │  200 ┤     ╭───╯  ╰──╮        ╭──╮              │   │
│  │      │    ╭╯          ╰──╮  ╭─╯  ╰─╮            │   │
│  │  100 ┤───╯               ╰──╯       ╰───        │   │
│  │      │                                           │   │
│  │    0 ┤───┬───┬───┬───┬───┬───┬───┬───┬───┬───   │   │
│  │      09  10  11  12  13  14  15  16  17  18      │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3. Настройки → Залы

CRUD-управление залами.

```
┌─────────────────────────────────────────────────────────┐
│  Settings          Залы  |  Детекция                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [+ Добавить зал]                                       │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Главный зал                                      │   │
│  │ Вместимость: 300  │  Камера: 10.0.0.101          │   │
│  │ Статус: ● Активен                                │   │
│  │                           [Изменить] [Удалить]   │   │
│  ├──────────────────────────────────────────────────┤   │
│  │ Зал A                                            │   │
│  │ Вместимость: 150  │  Камера: 10.0.0.102          │   │
│  │ Статус: ● Активен                                │   │
│  │                           [Изменить] [Удалить]   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  Форма добавления / редактирования:                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Название:    [Главный зал              ]         │   │
│  │ ID:          [hall-1                   ]         │   │
│  │ Вместимость: [300                      ]         │   │
│  │ URL камеры:  [rtsp://admin:pass@10.0.0.101...]   │   │
│  │ Активен:     [✓]                                 │   │
│  │                              [Сохранить]         │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 4. Настройки → Детекция

Параметры обработки — меняются на лету без перезапуска.

```
┌─────────────────────────────────────────────────────────┐
│  Settings          Залы  |  Детекция                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Модель YOLO                                            │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Модель:        [yolo26n.pt           ▼]          │   │
│  │                 yolo26n.pt (nano, быстрая)        │   │
│  │                 yolo26s.pt (small, точнее)        │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  Параметры детекции                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Уверенность (confidence):  [===●=====] 0.35      │   │
│  │ Интервал (сек):            [===●=====] 15        │   │
│  │ Сглаживание (alpha):       [==●======] 0.30      │   │
│  │ Размер кадра (imgsz):      [640            ▼]    │   │
│  │                             480 / 640 / 1280      │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  Статус системы                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Device:    cpu                                   │   │
│  │ Модель:    yolo26n.pt (загружена ✓)              │   │
│  │ Камер:     6 подключено / 6 всего                │   │
│  │ Uptime:    2ч 34мин                              │   │
│  │ Avg FPS:   0.4 кадров/сек                        │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│                                [Применить изменения]    │
└─────────────────────────────────────────────────────────┘
```

---

## API

### REST Endpoints

**Залы:**

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/rooms` | Список залов с текущими значениями |
| `GET` | `/api/rooms/{id}` | Детали зала |
| `POST` | `/api/rooms` | Создать зал |
| `PUT` | `/api/rooms/{id}` | Обновить зал |
| `DELETE` | `/api/rooms/{id}` | Удалить зал |

**Подсчёты:**

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/rooms/{id}/current` | Текущее значение |
| `GET` | `/api/rooms/{id}/history?hours=10` | История за N часов |
| `GET` | `/api/rooms/{id}/preview` | Последний кадр с bbox (base64) |

**Настройки:**

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/settings` | Текущие настройки детекции |
| `PUT` | `/api/settings` | Обновить настройки |
| `GET` | `/api/status` | Статус системы (uptime, камеры, модель) |

**WebSocket:**

| Endpoint | Описание |
|----------|----------|
| `ws://host:8000/ws/dashboard` | Real-time обновления всех залов |
| `ws://host:8000/ws/room/{id}` | Обновления + превью конкретного зала |

### Формат данных

**Текущее состояние зала:**
```json
{
  "room_id": "hall-1",
  "room_name": "Главный зал",
  "count": 127,
  "raw_count": 131,
  "capacity": 300,
  "occupancy_percent": 42.3,
  "status": "low",
  "timestamp": "2026-02-06T14:32:15Z"
}
```

**Live-превью:**
```json
{
  "room_id": "hall-1",
  "frame": "data:image/jpeg;base64,/9j/4AAQ...",
  "detections": 131,
  "timestamp": "2026-02-06T14:32:15Z"
}
```

**Настройки детекции:**
```json
{
  "model": "yolo26n.pt",
  "confidence_threshold": 0.35,
  "detection_interval": 15,
  "smoothing_alpha": 0.3,
  "imgsz": 640
}
```

### Статусы заполненности

| Статус | Диапазон | Цвет | Описание |
|--------|----------|------|----------|
| `empty` | 0% | Серый | Пусто |
| `low` | 1–39% | Зелёный | Свободно |
| `medium` | 40–69% | Жёлтый | Умеренно |
| `high` | 70–89% | Оранжевый | Заполняется |
| `full` | 90–100% | Красный | Почти полный |

---

## Конфигурация запуска

Минимальная стартовая конфигурация — только device и путь к БД. Всё остальное (залы, пороги, интервалы) настраивается через UI и хранится в SQLite.

### .env

```bash
DEVICE=cpu
DATABASE_PATH=./data/crowdcount.db
API_PORT=8000
FRONTEND_URL=http://localhost:3000
```

---

## Быстрый старт

### Требования

- Linux-сервер (Ubuntu 22.04+)
- Python 3.11+
- Node.js 18+
- Docker + Docker Compose (для продакшена)

### 1. Клонирование

```bash
git clone https://github.com/your-username/crowdcount.git
cd crowdcount
```

### 2. Тест детекции (без UI, быстрая проверка)

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Тест на фото
python ../scripts/test_photo.py --image photo.jpg

# Тест на видео
python ../scripts/test_video.py --video video.mp4

# Тест с веб-камерой
python ../scripts/test_webcam.py
```

### 3. Запуск в режиме разработки

```bash
# Терминал 1: Backend
cd backend
source venv/bin/activate
uvicorn main:app --reload --port 8000

# Терминал 2: Frontend
cd frontend
npm install
npm run dev
```

Открыть http://localhost:3000 → настроить залы и параметры через UI.

### 4. Продакшен (Docker)

```bash
docker compose up --build -d

# Дашборд: http://your-server:3000
# API:     http://your-server:8000/api/status
# Логи:   docker compose logs -f backend
```

---

## Оборудование

### Камеры

| Параметр | Значение |
|----------|----------|
| Модель | HiWatch DS-I853M |
| Разрешение | 8MP (3840×2160) |
| Угол обзора | 113° |
| Подсветка | Smart IR + LED, 30м |
| Питание | PoE (802.3af) |
| Протокол | RTSP, ONVIF |

**RTSP URL (субпоток 1080p):**
```
rtsp://admin:PASSWORD@10.0.0.101:554/Streaming/Channels/102
```

### Сеть

| Компонент | Модель | Кол-во |
|-----------|--------|--------|
| PoE свитч | TP-Link TL-SG1008MP (8 портов, 126W) | 1 |
| Патч-корды | Cat 5e/6 | 6–8 |

### Linux-сервер (минимальные требования)

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| CPU | 4 ядра | 8 ядер |
| RAM | 4 GB | 8 GB |
| Диск | 10 GB | 50 GB |
| GPU | Не требуется | — |
| Inference YOLO26n | ~40 ms/кадр (CPU) | — |

---

## Монтаж камер

Оптимальное размещение — **потолок, направление вниз (bird's eye view)**.

```
  Потолок (вид сверху)
  ┌────────────────────────────┐
  │                            │
  │          📷 Камера         │
  │       (центр, вниз)        │
  │                            │
  │   ┌──┐ ┌──┐ ┌──┐ ┌──┐    │
  │   │  │ │  │ │  │ │  │    │  ← ряды
  │   └──┘ └──┘ └──┘ └──┘    │
  │   ┌──┐ ┌──┐ ┌──┐ ┌──┐    │
  │   │  │ │  │ │  │ │  │    │
  │   └──┘ └──┘ └──┘ └──┘    │
  │                            │
  │      ┌──────────────┐      │
  │      │    Сцена     │      │
  │      └──────────────┘      │
  └────────────────────────────┘

  Высота: 3–5 метров
  Покрытие при 113°: ~8×8 м (на 4 м)
  Залы > 100 м²: 2 камеры
```

---

## Точность и калибровка

### Ожидаемая точность

| Сценарий | Точность |
|----------|----------|
| Bird's eye view, сидячий зал | 85–95% |
| Фронтальная камера | 70–85% |
| Плотная стоячая толпа | 60–80% |

### Рекомендуемые параметры

| Сценарий | `confidence` | `interval` | `alpha` |
|----------|-------------|-----------|---------|
| Сидячий зал | 0.30 | 30 сек | 0.2 |
| Стоячий ивент | 0.40 | 10 сек | 0.4 |
| Тестирование | 0.35 | 1–2 сек | 0.5 |

Все параметры настраиваются на лету через UI без перезапуска сервиса.

---

## Дорожная карта

- [x] Архитектура и документация
- [ ] Тестовые скрипты (фото / видео / веб-камера)
- [ ] Backend: детекция + API + SQLite
- [ ] Frontend: дашборд + настройки + live-превью
- [ ] Docker Compose
- [ ] Интеграция с IP-камерами
- [ ] Калибровка на реальных данных
- [ ] Алерты при переполнении
- [ ] Экспорт аналитики (CSV)
- [ ] Интеграция с EventOS

---

## Лицензия

MIT
