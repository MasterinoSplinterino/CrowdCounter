# Backend (internal only)
DEVICE=cpu
DATABASE_PATH=/app/data/crowdcount.db
API_PORT=8000
FRONTEND_URL=http://frontend:3000

# Frontend
# BACKEND_URL - internal Docker network URL for API proxy
BACKEND_URL=http://backend:8000
